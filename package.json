{
  "name": "revenge-ai-plugin",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=iife --global-name=revengePlugin --footer:js=\"revengePlugin.default;\" --outfile=index.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
